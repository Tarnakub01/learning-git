# 📚 คัมภีร์ Git สำหรับนักพัฒนา (Git Cheat Sheet)

สรุปคำสั่ง Git ที่ใช้บ่อยในชีวิตประจำวันของ Developer 🚀

---

## 1. 🎬 การเริ่มต้นและบันทึกประวัติศาสตร์ (Local Basics)
ใช้เมื่อเริ่มโปรเจกต์ใหม่ หรือต้องการบันทึกความคืบหน้า

* `git init` : **สร้างจักรวาล Git** (ทำครั้งเดียวตอนเริ่มโปรเจกต์)
* `git status` : **เช็กสถานะ** ดูว่ามีไฟล์ไหนแก้ เพิ่ม หรือลบไปบ้าง
* `git add <ชื่อไฟล์>` : **เตรียมบันทึก** (ถ้าเอาทุกไฟล์ใช้ `git add .`)
* `git commit -m "ข้อความ"` : **ถ่ายรูปเก็บประวัติ** (ข้อความควรสั้น กระชับ)
* `git log` : **ดูประวัติย้อนหลัง**

## 2. 🛡️ การรักษาความลับ (Security)
* สร้างไฟล์ `.gitignore` แล้วพิมพ์ชื่อไฟล์/โฟลเดอร์ที่ไม่ต้องการให้ Git จำลงไป (เช่น รหัสผ่าน, `secrets.txt`)

## 3. ☁️ การเชื่อมต่อยานแม่ (GitHub)
* `git remote add origin <URL>` : **ผูก Repository** ในเครื่องกับบน GitHub
* `git push -u origin main` : **ส่งโค้ดขึ้น Cloud** (ครั้งแรก)
* `git push` : **ส่งโค้ดขึ้น Cloud** (ครั้งต่อๆ ไป)

## 4. 🐑 การทำงานร่วมกันข้ามเครื่อง (Clone & Pull)
* `git clone <URL>` : **ดาวน์โหลดโปรเจกต์** จาก GitHub ลงเครื่อง
* `git pull` : **ดึงอัปเดตล่าสุด** จาก GitHub ลงมาทับในเครื่อง (ควรทำก่อนเริ่มเขียนโค้ดเสมอ)

## 5. 🌍 โลกคู่ขนาน (Branching)
**กฎเหล็ก:** *ห้ามเขียนโค้ดที่ยังไม่เสร็จลงใน `main` เด็ดขาด!*

* `git switch -c <ชื่อ Branch>` : **สร้างและสลับ** ไป Branch ใหม่ (เช่น `feature/login`)
* `git switch <ชื่อ Branch>` : **สลับ Branch** กลับไปมา
* `git merge <ชื่อ Branch>` : **รวมร่าง** ดึงโค้ดจาก Branch อื่นมารวมกับ Branch ที่ยืนอยู่
* `git branch -d <ชื่อ Branch>` : **ลบ Branch** ที่ใช้งานเสร็จแล้วทิ้ง

## 6. ⚔️ การปราบโค้ดชนกัน (Merge Conflict)
1. เมื่อสั่ง `git merge` แล้วขึ้น **CONFLICT**
2. เปิดไฟล์ที่มีปัญหา หาเครื่องหมาย `<<<<<<< HEAD` , `=======` , `>>>>>>>`
3. ลบเครื่องหมายทิ้งให้หมด และ **"เลือก/แก้ไข"** โค้ดบรรทัดนั้นให้ถูกต้อง
4. สั่ง `git add .` และ `git commit -m "Resolve conflict"` เพื่อปิดจ๊อบ